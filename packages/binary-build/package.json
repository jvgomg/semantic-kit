{
  "name": "@webspecs/binary-build",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "description": "Shared binary build utilities for webspecs packages",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "pretty": "prettier --write .",
    "pretty:check": "prettier --check .",
    "clean": "rm -rf tsconfig.tsbuildinfo"
  }
}
